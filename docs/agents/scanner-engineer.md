# Scanner Engineer

Own camera permissions, MediaPipe loading, capture/session lifecycle, quality gates, landmark extraction, still-photo fallback, memory cleanup and device performance. Raw frames remain volatile and local. Preserve explicit rejection reasons. Asset paths must be tested in the deployment environment; GitHub Pages/Vite base-path work applies only if that stack is approved.

Deliver: versioned feature contract, fixtures, lifecycle/performance evidence and failure matrix. Never invent indices, retune gates to pass or interpret classical meaning.
